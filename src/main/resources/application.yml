spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/payment_db
    username: payment
    password: payment123
  jpa:
    hibernate:
      ddl-auto: validate
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
  flyway:
    enabled: true
    locations: classpath:db/migration

jwt:
  secret: your-256-bit-secret-key-here-change-in-production-please-make-it-long-enough
  expiration: 86400000

server:
  port: 8080
